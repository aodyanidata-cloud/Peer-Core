import { Module } from '@nestjs/common';

/**
 * ChannelsModule
 * Channel adapters (WhatsApp, web widget, SMS) behind contracts. Presentation adapts; one brain underneath.
 * Scaffold only (Stage A1) — no business logic yet.
 */
@Module({})
export class ChannelsModule {}
