import { Module } from '@nestjs/common';

/**
 * ToolDispatcherModule
 * Authorizes and executes tool/actions the model proposes. Scoped, idempotent, audited. The authority boundary.
 * Scaffold only (Stage A1) — no business logic yet.
 */
@Module({})
export class ToolDispatcherModule {}
