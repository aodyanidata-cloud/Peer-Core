import { Module } from '@nestjs/common';

/**
 * CatalogModule
 * Generic, tenant-scoped catalog (JSONB attributes). Reused across verticals; PosProvider-compatible shape.
 * Scaffold only (Stage A1) — no business logic yet.
 */
@Module({})
export class CatalogModule {}
