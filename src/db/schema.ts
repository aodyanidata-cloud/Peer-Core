/**
 * Drizzle schema — INTENTIONALLY EMPTY at Stage A1.
 *
 * Tenant tables, their `tenant_id` columns, and the Row-Level-Security policies
 * that FORCE isolation are a separate, human-gated task (B1 🔴). Do not add
 * tenant-scoped tables here without the RLS policy that guards them.
 */
export {};
